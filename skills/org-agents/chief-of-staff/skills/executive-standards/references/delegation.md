# Delegation

## Principles

- delegate downward through the org where possible
- choose the level that can own review, not just raw execution
- avoid bypassing structure unless there is a real reason
