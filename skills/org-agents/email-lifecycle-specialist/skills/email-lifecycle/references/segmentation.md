# Segmentation

## Principles

- segmentation should match lifecycle reality, not arbitrary list splits
- state why a segment exists and what action it supports
- if segment rules are weak or ambiguous, say so clearly
