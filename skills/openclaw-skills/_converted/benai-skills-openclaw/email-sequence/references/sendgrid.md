# SendGrid Integration

The original BenAI skill linked to an external tools registry/integration doc outside the shared skill folder. That document was not bundled with the source snapshot used for conversion.

Treat this integration as an optional implementation target. Before using it in OpenClaw, verify the tool exists locally and document the sanctioned invocation path for this environment.
